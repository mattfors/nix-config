# nix-config

A collection of Nix configurations for reproducible development environments.

## Skein-env 🧶

A portable developer machine bootstrap environment with common development tools.

See [skein-env/README.md](skein-env/README.md) for details.

### Quick Start

```bash
cd skein-env
nix develop
```

Or use directly from GitHub:

```bash
nix develop github:mattfors/nix-config?dir=skein-env
```